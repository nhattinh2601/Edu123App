import { Text, View } from "react-native";

export default function Study () {
        return(
                <View>
                        <Text>Study</Text>
                </View>
        );
}